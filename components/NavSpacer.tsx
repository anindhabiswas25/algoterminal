/** Keeps the flow height the sticky header gives up, and carries the hero guide rules. */
export default function NavSpacer() {
  return (
    <div className="shell nav-spacer">
      <div className="v-rule left"></div>
      <div className="v-rule right"></div>

    </div>
  );
}
